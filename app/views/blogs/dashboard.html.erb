<%= render partial: 'staff_profile', locals: { own_profile: @own_profile,
                                      staff: current_staff_user,
                                      blogs: @staff_blogs } %>
